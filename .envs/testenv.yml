name: bhhh

channels:
  - defaults
  - conda-forge

dependencies:
  - python >=3.7
  - pip
  - pytest
  - pytest-cov

  # Package dependencies
  - numpy
  - pandas
  - statsmodels

  # Misc
  - black
  - pdbpp
  - pre-commit


  - pip:

    - -e ../ # install locally
