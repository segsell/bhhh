name: bhhh

channels:
  - defaults
  - conda-forge
  - opensourceeconomics

dependencies:
  - python >=3.10
  - pip
  - pytest
  - pytest-cov
  - numba

  # Package dependencies
  - numpy
  - pandas
  - statsmodels

  # Misc
  - black
  - pdbpp
  - pre-commit

  # Test dependencies
  - ruspy


  - pip:

    - -e ../ # install locally
