---
name: bhhh
channels:
  - defaults
  - conda-forge
  - opensourceeconomics
dependencies:
  - pip
  - pytest
  - pytest-cov
  - numba

  # Package dependencies
  - numpy
  - pandas
  - statsmodels

  # Misc
  - black
  - pdbpp
  - pre-commit

  # Test dependency
  - ruspy
  - pip:
      - -e ../  # install locally
